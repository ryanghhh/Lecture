

import pandas as pd
raw = pd.read_csv('penguins.csv') # 원본데이터 가지고있기
df = raw.copy() # 깊은 복사

# 데이터 획득 결과 확인
print('df.head(5):\n', df.head(5))
print('df.shape:', df.shape)
print('type(df):', type(df))


#   데이터 전처리: 결측치 처리 - 레이블 인코딩 - 더미변수 처리 - 이상치 제거 - 정규화
#   독립변수(입력): 범주형+연속형. 종속변수(출력): 범주형 --> 독립변수를 레이블 인코딩으로 통해 연속형으로 변환한 후 앙상블 기법 활용.

###################################
#   1-1. 데이터 전처리 - 결측치 처리
###################################

print(df.isna().sum()) #결측치있으면 true = 1. 없으면 0

print("값채우기")
df["bill_length_mm"] = df["bill_length_mm"].fillna(df["bill_length_mm"].median())
print(df.isna().sum())

print("값빼기")
df = df.dropna()
print(df.isna().sum())

print(df.shape)

###################################
#   1-2. 데이터 전처리 - 더미변수 변환
###################################

#범주형 -> 숫자로

from sklearn.preprocessing import LabelEncoder

le = LabelEncoder()

labels = ['island', 'sex']

for name in labels : 
    df[name] = le.fit_transform(df[name])

print(df)


df[species] = le.fit_transform(df[species])

print(df)


###################################
#   1-3. 데이터 전처리 - 인코딩 
###################################

print(df.dtypes)

for column in label :
    df[column] = df[column].astype('category')

print(df.dtypes)

df = pd.get_dummies(df)

print(df)

###################################
#   1-4. 데이터 전처리 - 이상치 제거
###################################



###################################
#   1-5. 데이터 전처리 - 정규화
###################################



###################################
#   1-6. 데이터 전처리 - 데이터 분리
###################################



###################################
#   2. 모형 훈련 및 적용
###################################



###################################
#   3. 모형 성능 검증
###################################



###################################
#   4. 하이퍼 파라미터 최적화
###################################

#   ex.) number of perceptrons in neural net
#   ex.) number of estimators in randomforest
#   and so on

###################################
#   5. 결과 정리
###################################



